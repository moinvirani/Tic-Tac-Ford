var game = {
  turn: "X",
  intstruct: function(str) {
      $('caption').text(str);
  }, 
  begin: function() {
    // reset things in case there is an existing game to clear
    $('body').off
  }

}